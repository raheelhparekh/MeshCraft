import Hero from "../components/Hero";

const HomePage = () => {
  return (
    <section className="h-screen w-full scroll-snap-center" id="Homepage">
      <Hero />
    </section>
  );
};

export default HomePage;